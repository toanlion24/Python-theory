# Thư viện Toán học và Tính toán
numpy>=1.24.0              # Tính toán số học, đại số tuyến tính
scipy>=1.10.0               # Khoa học tính toán, tối ưu hóa
sympy>=1.12                 # Toán học ký hiệu (symbolic math)
matplotlib>=3.7.0           # Vẽ đồ thị và biểu đồ
seaborn>=0.12.0             # Trực quan hóa dữ liệu nâng cao
plotly>=5.14.0              # Đồ thị tương tác

# Thư viện Giải thuật và Cấu trúc dữ liệu
networkx>=3.1               # Thuật toán đồ thị (graph algorithms)
sortedcontainers>=2.4.0     # Cấu trúc dữ liệu được sắp xếp
# heapq là module tích hợp sẵn trong Python, không cần cài đặt

# Thư viện AI và Machine Learning
scikit-learn>=1.3.0         # Machine learning cơ bản
pandas>=2.0.0               # Xử lý và phân tích dữ liệu
tensorflow>=2.13.0          # Deep learning (tùy chọn, có thể bỏ nếu không cần)
torch>=2.0.0                # PyTorch - Deep learning (tùy chọn)

# Thư viện Hỗ trợ Jupyter Notebook
ipywidgets>=8.0.0           # Widgets tương tác trong Jupyter
jupyter>=1.0.0              # Jupyter notebook
ipython>=8.12.0             # IPython nâng cao

# Thư viện Bổ sung
tqdm>=4.65.0                # Thanh tiến trình
imageio>=2.31.0             # Xử lý ảnh (cho visualization)
